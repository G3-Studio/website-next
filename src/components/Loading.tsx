import LoadingLogo from "./LoadingLogo";

export default function Loading() {
  
    return (
        <div className="flex items-center justify-center w-screen h-screen">
          <LoadingLogo stroke="#333333" fill="#EFEFEF" />
        </div>
    )
  }